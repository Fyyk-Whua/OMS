# AI_DEVELOPMENT_CHARTER

版本：1.0  
状态：EFFECTIVE  
生效日期：2026-07-14  
适用项目：`Fyyk-Whua/OMS`  
适用对象：ChatGPT、Codex、其他 AI 开发代理、人工开发者和代码审查者

---

## 1. 宪章目的

本宪章用于约束 AI 在 OMS 项目中的分析、设计、编码、测试、文档更新和 GitHub 协作行为。

目标：

1. 防止 AI 在上下文不足时擅自修改项目。
2. 防止需求、设计、代码和测试失去追踪关系。
3. 防止大范围修改导致项目不可恢复。
4. 确保关键决策经过人工确认并写入 GitHub。
5. 确保所有 AI 输出可检查、可复现、可回滚。
6. 确保 ChatGPT、Codex 和人工开发者能够持续交接。

---

## 2. 文档优先级

项目规则发生冲突时，按以下顺序处理：

1. 项目所有者的当前明确指令。
2. `AI_DEVELOPMENT_CHARTER.md`。
3. 已确认需求和已生效 ADR。
4. 当前任务实施计划。
5. `CODEX_WORKFLOW.md`。
6. 模块设计、API 合同和数据库设计。
7. 聊天记录、讨论摘要和 AI 建议。

无法判断时必须停止任务并请求人工决策，不得自行选择。

---

## 3. 最高原则

### 3.1 GitHub 是项目事实来源

正式需求、ADR、模块设计、API 合同、数据库设计、实施计划、当前状态、测试结果和代码实现，均以 GitHub 仓库中的内容为准。

聊天内容用于讨论和复盘，不能自动替代正式项目文件。

### 3.2 人工确认优先于 AI 推断

AI 不得自行把以下内容标记为已确认：

- 未明确确认的业务需求；
- AI 自行提出的方案；
- 推测的业务规则；
- 默认技术栈；
- 未授权的 GitHub 修改；
- 未授权的数据库、外部接口或部署操作。

需求状态统一使用：

- `CONFIRMED`
- `PROPOSED`
- `DISCUSSING`
- `REJECTED`
- `DEPRECATED`

只有项目所有者明确确认后，需求才能进入 `CONFIRMED`。

### 3.3 小步开发

每次 AI 任务只处理一个边界清晰、可独立验证的小任务。

每个任务必须能够独立说明、测试、提交、审查和回滚。

禁止在一个任务中混合多个独立模块、大规模重构、技术栈迁移、数据库迁移和无关优化。

### 3.4 先设计，后编码

业务代码实现前，应根据任务复杂度完成必要的：

- 需求说明；
- ADR；
- 数据模型；
- API 合同；
- 状态机；
- 实施计划；
- 验收标准。

核心边界未确认时，不得直接编写业务代码。

### 3.5 测试先行

新增功能和缺陷修复应执行：

1. 编写失败测试；
2. 运行并确认测试按预期失败；
3. 实现满足测试的最小代码；
4. 运行目标测试；
5. 运行相关回归测试；
6. 更新文档；
7. 提交代码。

没有实际测试证据时，不得声称“已完成”“已修复”或“测试通过”。

---

## 4. 项目边界

### 4.1 OMS 职责

OMS 负责：

- 订单运营和审核；
- 库存策略和库存占用；
- 预售和渠道库存；
- 履约编排；
- ERP/WMS 集成协调；
- 文件安全和数据导入；
- 异常补偿和业务审计。

### 4.2 非项目职责

OMS 不应无边界扩展为：

- 完整 ERP；
- 完整 WMS；
- 财务总账系统；
- 采购管理系统；
- 生产制造系统；
- 通用低代码平台。

ERP 模式下 ERP 是库存权威；WMS 负责仓内作业；OMS 负责订单运营和履约协调。

---

## 5. 任务开始前的强制检查

AI 开始任何开发任务前必须读取：

1. `AI_DEVELOPMENT_CHARTER.md`
2. `README.md`
3. `CURRENT_STATUS.md`
4. `NEXT_TASK.md`
5. `CODEX_WORKFLOW.md`
6. 当前任务对应的需求文件
7. 当前任务对应的 ADR
8. 当前任务对应的模块设计
9. 当前任务对应的实施计划
10. 最近相关提交记录

开始前必须确认：

- 当前唯一下一任务；
- 允许修改的文件和目录；
- 禁止修改的文件和目录；
- 是否允许安装依赖；
- 是否允许连接数据库；
- 是否允许调用外部 API；
- 是否允许执行 GitHub 写操作；
- 是否存在互相冲突的需求或决策。

存在冲突时必须停止实现并报告。

---

## 6. 授权规则

### 6.1 GitHub 写操作

在未获得项目所有者明确授权时，AI 不得执行：

- 创建、修改、删除或重命名正式文件；
- 创建分支或 Pull Request；
- 合并 Pull Request；
- 修改仓库设置、可见性和权限；
- 新增 GitHub Actions；
- 修改 CI 权限；
- 修改依赖版本。

授权只对明确指定的任务和范围有效，不得自动扩大。

### 6.2 高风险操作

以下操作必须单独确认：

- 删除或迁移数据；
- 执行数据库迁移；
- 修改生产配置；
- 修改密钥和权限；
- 向外部系统发送真实业务数据；
- 执行真实订单或库存操作；
- 自动部署或自动合并；
- 强制推送或覆盖提交历史。

---

## 7. 需求管理规则

每项正式需求至少应包含：

- 需求编号；
- 标题和状态；
- 优先级；
- 业务背景；
- 前置条件；
- 主流程和异常流程；
- 权限和数据规则；
- 验收标准；
- 对应 ADR、设计、任务和测试。

需求变化时必须记录变更原因。被否定需求标记为 `REJECTED`，被替代需求标记为 `DEPRECATED`，不得通过删除历史内容掩盖决策变化。

---

## 8. 架构决策规则

重大业务或技术决策必须建立 ADR，包含：

- 背景；
- 可选方案；
- 选择结果；
- 选择原因；
- 影响范围；
- 风险；
- 回滚条件；
- 状态。

以下事项原则上必须建立 ADR：

- 技术栈和数据库；
- 多租户模型；
- 库存权威模型；
- 订单和履约状态机；
- ERP/WMS 集成方式；
- 幂等和补偿机制；
- 文件安全模型；
- 部署架构；
- Redis 和消息队列的引入。

---

## 9. 代码修改规则

### 9.1 最小修改原则

AI 只能修改完成当前任务所必需的最小文件集合。

禁止无关格式化、无关重命名、大范围移动文件、未经批准的重构、修改无关模块和引入无关依赖。

### 9.2 清晰边界

模块应具有单一职责、明确接口、显式依赖和可独立测试能力。

避免巨型服务类、隐式全局状态、跨模块直接访问数据库、在路由层实现核心业务，以及复制库存和权限计算逻辑。

### 9.3 禁止静默降级

当指定真实实现尚未完成时，应明确失败。

禁止：

- 自动回退到内存模式；
- 自动忽略外部系统错误；
- 将未知状态视为成功；
- 将网络超时视为业务失败；
- 吞掉异常后返回正常结果。

---

## 10. 数据和安全规则

### 10.1 敏感信息

禁止提交密码、API Key、Token、私钥、真实数据库凭据、客户隐私数据、生产环境配置和未脱敏聊天记录。

敏感配置应使用环境变量、本地安全存储、GitHub Secrets 或专用密钥管理服务。

### 10.2 文件安全

所有上传文件必须经过统一文件安全网关。业务模块不得绕过文件类型、MIME、Magic Number、大小、病毒扫描、摘要、Excel 风险、隔离区和权限校验。

### 10.3 多租户隔离

所有业务查询和写入必须包含租户边界。

企业、店铺和库存组织不选择时，只能查询当前用户授权范围内的全部数据，不能查询系统全量数据。权限校验必须在后端执行。

---

## 11. 库存开发特别约束

### 11.1 库存权威

- OMS 独立模式：OMS 是库存权威。
- ERP 模式：ERP 是库存权威。
- OMS 不得擅自修正 ERP 正式库存。
- ERP 网络超时不等于 ERP 失败。

### 11.2 库存计算

所有模块必须调用统一可售库存计算服务，不得在订单、商品、渠道或活动模块中复制库存公式。

### 11.3 现货和预售

现货库存与预售额度必须分别计算、展示、占用和审计，禁止把预售额度伪装成物理库存。

### 11.4 库存流水

所有库存变化必须生成流水。已完成流水不得直接修改或删除，错误通过冲销和重新入账修正。流水长期保留，不做业务归档。

### 11.5 库存占用

库存查询、校验和占用必须原子执行。

ERP 结果为 `UNKNOWN` 时，必须保留 OMS 软占用并启动查询补偿，不得立即释放；无法自动确认时转人工处理。

---

## 12. AI 输出要求

每次任务完成后必须报告：

1. 任务目标；
2. 实际修改文件；
3. 未修改范围；
4. 实现内容；
5. 测试命令；
6. 测试结果；
7. 风险和限制；
8. 未完成内容；
9. 回滚点；
10. 唯一下一任务。

不得只输出“已完成”“应该可以”或没有命令和结果的“测试通过”。

---

## 13. Git 和 Pull Request 规则

### 13.1 分支命名

```text
feature/<requirement-id>-<short-name>
fix/<issue-id>-<short-name>
docs/<topic>
chore/<topic>
```

### 13.2 提交信息

```text
feat(inventory): implement REQ-INVENTORY-044 reservation lifecycle
fix(inventory): prevent release on unknown ERP result
docs(adr): add ADR-031 technology stack
test(inventory): cover reservation idempotency
```

一个提交只表达一个逻辑变更。

### 13.3 PR 要求

每个 PR 必须说明：

- 关联需求和 ADR；
- 变更范围和设计说明；
- 测试命令和结果；
- 数据库、API 和安全影响；
- 回滚方式；
- 未完成项。

合并前必须确认 CI 通过、无敏感信息、文档已同步、状态文件已更新且下一任务唯一明确。

---

## 14. 文档同步规则

每个确认或完成的任务，应根据影响更新：

- `CURRENT_STATUS.md`
- `NEXT_TASK.md`
- 对应需求文件
- 对应 ADR
- 对应模块设计
- API 合同
- 数据库设计
- 追踪矩阵
- 测试清单
- 会话决策摘要

禁止代码和项目状态文档长期不一致。

---

## 15. 中断恢复规则

Codex 额度耗尽、会话中断、环境关闭或上下文丢失后，恢复前必须重新读取：

1. 本宪章；
2. `CURRENT_STATUS.md`；
3. `NEXT_TASK.md`；
4. 对应需求、ADR 和计划；
5. 当前分支状态；
6. 最近提交；
7. 未完成测试结果。

不得只凭聊天记忆继续开发。

---

## 16. 完成定义

任务只有同时满足以下条件才能标记为完成：

- 需求明确；
- 设计已确认；
- 代码已实现；
- 测试已执行且结果可复现；
- 文档已同步；
- Git 提交已完成；
- 风险已说明；
- 无未授权修改；
- 下一任务已明确。

不满足时使用：

- `IN_PROGRESS`
- `BLOCKED`
- `PARTIALLY_COMPLETED`

---

## 17. AI 强制停止条件

遇到以下情况必须停止并请求人工决策：

- 需求互相冲突；
- ADR 与需求冲突；
- 技术栈未确认；
- 权限不足；
- 可能丢失数据；
- 可能暴露敏感信息；
- 需要新增高风险依赖；
- 需要修改生产环境；
- 测试结果与预期不一致；
- 任务范围明显扩大；
- 当前唯一下一任务不明确。

---

## 18. 宪章修改规则

本宪章是项目最高级协作约束之一。

任何修改必须：

1. 提出修改原因；
2. 明确影响范围；
3. 获得项目所有者确认；
4. 更新版本号和修改日期；
5. 保留修改记录；
6. 使用独立 Git 提交。

AI 不得自行修改、弱化或绕过本宪章。

---

## 19. 生效

本宪章已经项目所有者确认，自提交到 GitHub 起生效。
